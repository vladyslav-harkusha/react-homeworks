import { FC } from "react";
import './App.css';

export const App: FC = () => {

    return (
      <h1 className="text-3xl font-bold underline">
          Clear react-vite template
      </h1>
    );
};
